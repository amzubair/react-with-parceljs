import React, { Component } from "react";

const styles = {
  fontFamily: "sans-serif",
  textAlign: "center"
};

class App extends Component {
  render() {
    return (
      <div style={styles}>
        <h2>React with ParcelJS</h2>
      </div>
    );
  }
}

export default App;
